# 更新日志

记录格式：**改了什么** + **为什么**。第二项才是这份文件的价值——数值在
`tokens/` 里随时查得到，理由不写下来就没了。

---

## 1.0.0 — 2026-08-31

首个版本。从 wutong.org 的 `src/styles/theme.css` 提取，拆成 core / site 两层。

### 字体

- **中文标题不再加载网络字体。** 实测一页要下 249.5KB 分片，只为渲染几十个
  中文字；标题的性格主要来自拉丁展示体，中文黑体之间的差异读者基本感知不到，
  而 250KB 的首屏延迟感知得到。现在中文走系统黑体（Mac 苹方 / Win 微软雅黑）。
- 站上唯一的网络字体是 **Funnel Display**（34KB，OFL）。
- **移除 HarmonyOS Sans SC。** 华为许可证只授权分发「未修改的副本」，而站上
  那套是 pyftsubset 子集化的产物；许可证另有一条禁止字体以独立形式再分发，
  设计系统要进公开仓库，这两条都过不去。
- 中途换过思源黑体（OFL 合规），但体积问题依旧，一并取消。

### 颜色

- **主色改为品牌海军蓝 `#2d3782`**（原 `#1e3a8a`）。同为海军蓝但色相偏了，
  品牌那支更靛。`jacksons-purple` 整条 ramp 按新基色重算：浅三档是基色混白
  89.8% / 79.7% / 29.9%，深三档是基色乘 0.797 / 0.399 / 0.297——明暗关系与
  原来完全一致，只有色相位移。
- 新增交互蓝 `--color-brand-blue #165dfc` / `-deep #0e48d6`。不在品牌原板上，
  取自 deals.meticsmedia.com 的 `--indigo`（读它自己的 `:root`，非截图取色）。

### 字重

- **`--font-weight-bold` 从 600 改为 700。** 原本它和 semibold 同为 600，等于
  这套系统里没有「粗体」——当时整页 145 个文字节点有 95 个落在 600 上，中文
  在这个密度下会糊成一块。

### 圆角

- **`--radius-card` 从 24px 收到 16px。** 24px 在小卡上会圆到发胖。一处生效，
  影响全站 23 个用到 `rounded-card` 的地方。
- 新增 `--radius-mark 6px`（24px 品牌图标方块）。

### 删除

- **`.scheme-1` 到 `.scheme-4`。** Relume 模板导入时留下的区块配色方案，全站
  零使用。连带 `--color-ink-15` 被摇掉（它唯一的消费者是 `--scheme-border`）。

### 拆分

- `core.css` / `site.css` 两层。工具列表页那套移植自 deals.meticsmedia.com 的
  配色（`--color-ref-*`）、卡片字号、chip 与卡片组件类，全部归入站点层——它们
  跟品牌色是两个体系，不能互相推导。
- 拆分后与拆分前的构建产物做过 token 级和规则级比对：**除有意删除的
  `.scheme-*` 外，零差异。**

### 已知缺口

- **焦点态几乎不存在。** 全站只有三处定义了 `:focus-visible`，主按钮、次按钮、
  chip、卡片链接、导航项全部没有可见焦点环。文档第 10 节给了建议规范
  （`--color-focus #b94d2f` / `--color-focus-inverse #ffffff`），尚未实现。
- **现有焦点环颜色不达标。** `--color-secondary #f2673f` 作为焦点环在白底上
  3.09:1 勉强过 3:1 的图形阈值，但在 `surface-alt`(2.96) 和 `ref-bg`(2.99) 上
  都低于阈值——站上用得最多的正是这两个底。
- 图标尺寸（六档）和 z-index（五档）仍是散在组件里的裸数字，未令牌化。

## 1.1.0 — 2026-08-31

站点色板向 logo 对齐。

### 为什么

拿到 logo 源文件 `wordmark-landscape.svg` 之后发现，logo 里的三个颜色**一个都不在
站点色板里**：

```
logo 蓝   #003a89  vs 站点主色 #2d3782    色距 45.6
logo 红   #e63946  vs 站点 punch #d93730  色距 25.6
logo 青灰 #457b9d  vs 站点无对应          色距 ≥84.8
```

`#457b9d` / `#e63946` / `#a8dadc` 是一套成组的配色，而站点的 `--color-aqua-island`
正好就是 `#a8dadc` —— 说明 logo 用的是这一套，站点的蓝和红后来漂走了。

logo 是品牌里最固定的一环，UI 色板应该从它推导，而不是反过来。所以站点向 logo 靠拢。

### 改了什么

- **`--color-jacksons-purple` 基色 `#2d3782` → `#003a89`**（主色）
- **`--color-punch` 基色 `#d93730` → `#e63946`**
- **新增 `--color-steel` 完整色阶**，基色 `#457b9d`
- 三条色阶都按这套设计原有的步长重算，明暗关系不变，只有色相位移：
  浅三档混白 89.8% / 79.7% / 29.9%，深三档乘 0.797 / 0.399 / 0.297

### 连带处理

- **`--color-error` 从 `punch` 改指 `punch-dark`。** 白字压新基色 `#e63946` 只有
  4.17:1，过不了 AA（旧的 `#d93730` 是 4.63）。`punch-dark #b72d38` 是 6.09:1。
  基色本身留作图形填充 —— 它是 logo 的真实颜色，不能改。
  这跟珊瑚配白字被 AA 压出 `#b94d2f` 是同一类处理。

### 复验

换色后重跑了全部关键对比度，除有意避开的 punch 基色外全部达标：

```
主按钮 #003a89 / 白           10.68  ✓（原 10.60）
主按钮 hover #002e6d / 白     13.03  ✓
深色区块次级文字 #002e6d/#cbd7e7  8.94  ✓（原 8.90）
info-light #e5ebf3 / 主色字    8.90  ✓
error #b72d38 / 白             6.09  ✓
steel #457b9d / 白             4.59  ✓
```

`error` / `punch` 此前站上一处都没用到，所以这次没有现网影响。

### 品牌资产

同期收进完整 logo 库：横版矢量字标（主资产）、竖版、纯图标，各含黑/白单色变体，
以及官方 favicon（96/192/512）。竖版和纯图标仍无 SVG（源文件是 .ai，留在 Zoho）。
